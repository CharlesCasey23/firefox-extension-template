{
  "name": "firefox-extension-template",
  "version": "0.0.1",
  "description": "This is a Firefox web browser extension template.\nThis extension uses Manifest V.2.",
  "scripts": {
    "build": "rm -rf dist >/dev/null && webpack --config webpack.config.js",
    "dev": "npm run build && web-ext run -s './dist' --devtools"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/firefox-webext-browser": "^111.0.1",
    "@types/node": "^20.3.1",
    "copy-webpack-plugin": "^11.0.0",
    "ts-loader": "^9.4.3",
    "typescript": "^5.1.3",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4"
  }
}
